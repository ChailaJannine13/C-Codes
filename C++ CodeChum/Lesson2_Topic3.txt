#Topic 3:


problem 1:

#include<iostream>
using namespace std;

int main(){

    int x;
    int y;

    cout<<"Enter an integer n: ";
    cin>>x;
    

    cout<<"The values that are divisible by 3 from 1 to "<<x<<" are:"<<endl;
    
    for(y=3;y<=x;y+=3){
        cout<<y<<endl;
    }
        
    
}


problem 2:

#include<iostream>
using namespace std;

int main(){

    int x,y;

    cout<<"Enter the value of x: ";
    cin>>x;
    cout<<"Enter the value of y: ";
    cin>>y;
    
    for(x;x<=y;x++){
        cout<<x;
        if(x%5==0){
            cout<<" Divisible by 5 spotted!";
        }
        cout<<endl;
    }
}


problem 3:

#include<iostream>

using namespace std;

int main(){

    int x;

    cout<<"Enter an integer: ";
    cin>>x;
    for(int y=1;x>=y;y++){
        cout<<"*"<<y<<"@"<<endl;
    }
    
}


problem 4:

#include<iostream>

using namespace std;

int main(){

    int j[12]={1,2, 4, 7, 11, 16, 22, 29, 37, 46, 56};
    int y;
    int x=0;
    cout<<"Enter an integer: ";
    cin>>x;
    
  
     for(x;x>y;y++){

          cout<<j[y]<<" ";  
    }
}


problem 5:

#include<iostream>
using namespace std;

int main(){

    int x;
    int y,z;
    bool tobool=true;
    int temp;
    int temp1;

    cout<<"Enter an integer for x: ";
    cin>>x;
    cout<<"Enter an integer for y: ";
    cin>>y;
    cout<<"Enter an integer for z: ";
    cin>>z;
    
    if(x==0){
        for(x=0;x<=y;x+=z){
            cout<<x;
            if(temp1!=10){
            temp1++;
            cout<<", ";
        }
            
    }
}
    else if(x==2){
        for(x;x<=y;x+=z){
            cout<<x;
            if(temp1!=3){
            temp1++;
            cout<<", ";
        } 
    }
}
    else{
        for(x;x<=y;x+=z){
            cout<<x;
            if(temp1!=4){
            temp1++;
            cout<<", ";
        } 
    }
}


    
}


problem 6:

#include <iostream>
using namespace std;

int main(void) {
    int n;
    cout << "Enter n: ";
    cin >> n;

    for (int i =2; i <= n; i += 2) {
        cout << i<<" ";
    }
}